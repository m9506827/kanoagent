# KanoAgent Git 工作流程說明

## 📋 概述

本文檔說明 KanoAgent 專案中檔案保存、提交和推送的時機與方式。

## 💾 檔案保存（Save）

### Cursor 編輯器自動保存

**何時保存：**
- ✅ **自動保存**：Cursor 預設會自動保存檔案
  - 當您停止編輯一段時間後（預設約 1-2 秒）
  - 當您切換到其他檔案時
  - 當您關閉檔案時
- ✅ **手動保存**：
  - 按 `Ctrl+S` (Windows/Linux) 或 `Cmd+S` (Mac)
  - 選擇選單：`File > Save`

**保存位置：**
- 檔案直接保存到本地磁碟（`D:\Google\KanoAgent\`）
- **不會自動提交到 Git**

### 重要提醒

⚠️ **保存 ≠ 提交**
- 保存只是將變更寫入本地檔案
- 保存不會自動建立 Git commit
- 保存不會自動推送到遠端倉庫

## 📝 Git Commit（提交）

### 何時 Commit

**需要手動執行：**
- Git commit 是**手動操作**，不會自動執行
- 您需要明確執行 commit 命令才會建立提交

### Commit 方式

#### 方法 1：使用 Cursor 內建的 Git 功能

1. **開啟 Source Control 面板**
   - 按 `Ctrl+Shift+G` (Windows/Linux) 或 `Cmd+Shift+G` (Mac)
   - 或點擊左側工具列的 Source Control 圖示

2. **暫存變更（Stage）**
   - 點擊檔案旁的 `+` 號，或
   - 點擊 "Stage All Changes"

3. **輸入 Commit 訊息**
   - 在訊息框中輸入描述性的 commit 訊息
   - 例如：`feat: 新增 Senior Pre-sales Consultant Agent`

4. **提交**
   - 按 `Ctrl+Enter` 或點擊 "Commit" 按鈕

#### 方法 2：使用終端機命令

```bash
# 1. 檢查變更狀態
git status

# 2. 暫存所有變更
git add .

# 或暫存特定檔案
git add agents/product.py
git add tasks/tasks.py

# 3. 提交變更
git commit -m "feat: 新增 Senior Pre-sales Consultant Agent"

# 或使用詳細訊息
git commit -m "feat: 新增 Senior Pre-sales Consultant Agent

- 新增 SeniorPreSalesConsultantAgent
- 新增需求澄清任務
- 更新 PRD 生成任務
- 將系統改為通用型"
```

### Commit 訊息規範建議

使用語義化提交訊息（Conventional Commits）：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**類型（Type）：**
- `feat`: 新功能
- `fix`: 修復 bug
- `docs`: 文檔變更
- `style`: 程式碼格式（不影響功能）
- `refactor`: 重構
- `perf`: 效能優化
- `test`: 測試相關
- `chore`: 建置或工具變更

**範例：**
```
feat(product): 新增 Senior Pre-sales Consultant Agent

- 新增 SeniorPreSalesConsultantAgent 到 agents/product.py
- 新增 config/presales_questions.py 問題清單
- 更新 tasks/tasks.py 新增需求澄清任務
```

## 🚀 Git Push（推送到遠端）

### 何時 Push

**需要手動執行：**
- Git push 是**手動操作**，不會自動執行
- 即使您 commit 了，也不會自動推送到遠端

### Push 方式

#### 方法 1：使用 Cursor 內建的 Git 功能

1. **在 Source Control 面板**
   - 點擊右上角的 `...` 選單
   - 選擇 "Push" 或 "Push to..."

2. **或使用快捷鍵**
   - 按 `Ctrl+Shift+P` 開啟命令面板
   - 輸入 "Git: Push" 並執行

#### 方法 2：使用終端機命令

```bash
# 推送到預設遠端分支
git push

# 推送到特定遠端和分支
git push origin main

# 或
git push origin master
```

### 首次 Push 設定

如果這是第一次推送到遠端倉庫：

```bash
# 1. 添加遠端倉庫
git remote add origin <repository-url>

# 例如：
# git remote add origin https://github.com/username/KanoAgent.git

# 2. 推送到遠端
git push -u origin main
```

## 📊 完整工作流程

```
編輯檔案
    ↓
自動保存（Cursor）
    ↓
檔案變更在本地
    ↓
手動執行 git add
    ↓
手動執行 git commit
    ↓
變更提交到本地 Git 倉庫
    ↓
手動執行 git push
    ↓
變更推送到遠端倉庫（GitHub/GitLab 等）
```

## ⚙️ 檢查 Git 狀態

### 檢查是否有未提交的變更

```bash
git status
```

### 檢查提交歷史

```bash
git log
```

### 檢查遠端倉庫狀態

```bash
git remote -v
```

## 🔒 安全提醒

### 不會自動提交的檔案

根據 `.gitignore`，以下檔案**不會被提交**：

- `.env` - 環境變數（包含 API Key）
- `output/` - 輸出目錄
- `__pycache__/` - Python 快取
- `*.log` - 日誌檔案

### 重要提醒

⚠️ **永遠不要提交敏感資訊：**
- API Keys
- 密碼
- 個人資訊
- `.env` 檔案

✅ **使用 `.env` 檔案管理敏感資訊：**
- `.env` 已在 `.gitignore` 中
- 使用 `config.example.env` 作為範本

## 🛠️ 初始化 Git 倉庫（如果尚未初始化）

如果專案還沒有 Git 倉庫：

```bash
# 1. 初始化 Git 倉庫
git init

# 2. 添加所有檔案（.gitignore 中的除外）
git add .

# 3. 建立首次提交
git commit -m "chore: 初始化 KanoAgent 專案"

# 4. 添加遠端倉庫（可選）
git remote add origin <repository-url>

# 5. 推送到遠端（可選）
git push -u origin main
```

## 📚 相關資源

- [Git 官方文檔](https://git-scm.com/doc)
- [Conventional Commits 規範](https://www.conventionalcommits.org/)
- [Cursor Git 整合文檔](https://cursor.sh/docs)

## ❓ 常見問題

### Q: Cursor 會自動 commit 嗎？
**A:** 不會。Cursor 只會自動保存檔案，不會自動 commit。

### Q: 如何設定自動 commit？
**A:** 不建議設定自動 commit。應該手動 review 變更後再 commit。

### Q: 如何撤銷未提交的變更？
**A:** 
```bash
# 撤銷工作區的變更（未暫存）
git checkout -- <file>

# 撤銷所有未暫存的變更
git checkout -- .
```

### Q: 如何撤銷已提交但未推送的 commit？
**A:**
```bash
# 撤銷最後一次 commit（保留變更）
git reset --soft HEAD~1

# 撤銷最後一次 commit（不保留變更）
git reset --hard HEAD~1
```

### Q: 如何查看變更內容？
**A:**
```bash
# 查看工作區變更
git diff

# 查看已暫存的變更
git diff --staged

# 查看提交歷史
git log --oneline
```
